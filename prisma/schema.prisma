// For testing

datasource db {
    provider = "postgresql"
    url = env("PRISMA_DATABASE_URL")
}

model Item {
    id Int @id @default(autoincrement())
    name String
    description String
    price Int
}
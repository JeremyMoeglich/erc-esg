// For testing

datasource db {
    provider = "postgresql"
    url = "postgresql://postgres:postgres@localhost:5432/postgres"
}

model Item {
    id Int @id @default(autoincrement())
    name String
    description String
    price Int
}